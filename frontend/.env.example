# Django API base (no trailing slash)
VITE_API_URL=http://127.0.0.1:8000/api

# Optional: override server payment QR (UPI VPA). If unset, uses GET /api/payment-config/
# VITE_UPI_PA=yourcafe@paytm
# VITE_UPI_PAYEE_NAME=Mudcup
